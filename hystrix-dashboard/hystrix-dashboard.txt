hystrix-dashboard学习：
1，jar包版本要对应，不然报错。
2，如果hsstri-dashboard一直显示loading，则消费者需要访问服务才会有监控图像（Loading...是一直在等待负载均衡的提供方要去消费服务，即访问负载均衡服务器，去调用客户端，如果有数据响应则监控界面就会有图形数据展示：
如果想让图中的数据发生变化，则需要循环多次的去访问负载均衡的提供方，让其消费服务，以至于达到监控的目的）。